# 习题 7-2
```text
在示例程序中的 HTMLBuilder 类中，需要首先调用 makeTitle 方法，但是在 TextBuilder 类中，则对方法调用的顺序没有要求。
请修改 Builder 类、TextBuilder 类和 HTMLBuilder 类，确保“在调用 makeString 方法、makeItems 方法和 close 方法
之前必须且只能调用一次 makeTitle 方法”。
```

